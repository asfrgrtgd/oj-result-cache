services:
  oj-hash:
    build: .
    container_name: oj-hash
    working_dir: /app
    volumes:
      - .:/app
    command: ["python", "oj_hash.py"]
    environment:
      - TERM=xterm-256color
    tty: true
    stdin_open: true
