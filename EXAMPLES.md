# 正規化ハッシュ：同一視される例・されない例

## ✅ 同一視される例（同じハッシュ値になる）

### 例1: 空白・改行・インデントの違い
```cpp
// パターンA
int sum(int a, int b) {
    return a + b;
}
```

```cpp
// パターンB（同一視される）
int sum(int a,int b){return a+b;}
```

```cpp
// パターンC（同一視される）
int sum ( int a , int b ) 
{
    return a + b ;
}
```

**理由:** トークンストリームは同じ `int` `sum` `(` `int` `a` `,` `int` `b` `)` `{` `return` `a` `+` `b` `;` `}`


### 例2: コメントの有無
```cpp
// パターンA
int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
}
```

```cpp
// パターンB（同一視される）
// 階乗を計算する関数
int factorial(int n) {
    // ベースケース
    if (n <= 1) return 1;
    // 再帰呼び出し
    return n * factorial(n - 1);  // nとn-1の積
}
```

**理由:** コメントは `TokenKind.COMMENT` として除外される


### 例3: 括弧の改行スタイル
```cpp
// パターンA (K&Rスタイル)
void process() {
    for (int i = 0; i < 10; i++) {
        cout << i << endl;
    }
}
```

```cpp
// パターンB（同一視される - Allmanスタイル）
void process() 
{
    for (int i = 0; i < 10; i++) 
    {
        cout << i << endl;
    }
}
```

**理由:** トークンの順序と内容が同じ


---

## ❌ 同一視されない例（異なるハッシュ値になる）

### 例1: 変数名の変更
```cpp
// パターンA
int sum(int a, int b) {
    return a + b;
}
```

```cpp
// パターンB（異なる）
int sum(int x, int y) {
    return x + y;
}
```

**理由:** `IDENTIFIER`のスペリングが `a`, `b` から `x`, `y` に変化


### 例2: リテラル値の変更
```cpp
// パターンA
int limit = 100;
```

```cpp
// パターンB（異なる）
int limit = 200;
```

**理由:** `LITERAL`トークンの値が `100` から `200` に変化


### 例3: 関数名の変更
```cpp
// パターンA
int add(int a, int b) {
    return a + b;
}
```

```cpp
// パターンB（異なる）
int sum(int a, int b) {
    return a + b;
}
```

**理由:** 関数名のスペリングが `add` から `sum` に変化


### 例4: ロジックの変更
```cpp
// パターンA（ループ）
int sum = 0;
for (int i = 1; i <= n; i++) {
    sum += i;
}
```

```cpp
// パターンB（異なる - 数式）
int sum = n * (n + 1) / 2;
```

**理由:** トークン列が完全に異なる


### 例5: 演算子の変更
```cpp
// パターンA
return a + b;
```

```cpp
// パターンB（異なる）
return a * b;
```

**理由:** 演算子トークンが `+` から `*` に変化


### 例6: 順序の変更
```cpp
// パターンA
int a = 10;
int b = 20;
```

```cpp
// パターンB（異なる）
int b = 20;
int a = 10;
```

**理由:** トークンの出現順序が変わる


---

## 🔍 重要な注意点

### このツールで検出できること
- ✅ **完全なコピペ**（空白・コメントを変えただけ）
- ✅ **コードスタイルの違い**（インデントや改行のみ）

### このツールで検出できないこと
- ❌ **変数名を変えたコピペ**（`a` → `x`など）
- ❌ **リテラル値を変えたコピペ**（`100` → `200`など）
- ❌ **意味的に同じだがコードが異なる実装**（ループ vs 数式など）
- ❌ **関数名を変えたコピペ**

### より高度な検出には
より巧妙な不正検出には以下が必要：
1. **AST（抽象構文木）ベースの比較**：変数名を無視
2. **制御フローグラフの比較**：ロジック構造の類似性
3. **意味解析**：等価な異なる実装の検出
